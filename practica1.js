let saludo = prompt("Ingresa un saludo");
saludo = saludo.toLowerCase();

if (saludo === "hola" || saludo === "buenos dias") {
    alert("Bien, gracias por saludar");
} else {
    alert("¡Que mal!, saluda primero");
}

// Operadores lógicos
// || o
// && y 

// JS es sensible a mayúscula y minúsculas
// se recomienda usar minúsculas

