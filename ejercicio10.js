// Condicionales ifelse

let nombre = prompt("Ingresa tu nombre");
let edad = prompt("Ingresa tu edad");

if (edad>=18) {
    alert("Bienvenida " + nombre + " Tienes acceso al catálogo de películas con contenido violento.");
} else {
    alert("Hola " + nombre + " No tienes acceso al catálogo de películas con contenido violento.");
}

/** Este es un comentario en bloque */
/**
 * > Mayor
 * < Menor
 * >= Mayor igual (cuando trabajo con números)
 * >= Menor igual (cuando trabajo con números)
 * == igual ( si pongo solo un = es asignación)
 */